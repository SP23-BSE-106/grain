# TODO List for Fixing Hydration Error

- [x] Add `suppressHydrationWarning={true}` to the body element in `app/layout.tsx` to suppress hydration warnings caused by browser extensions injecting attributes.
